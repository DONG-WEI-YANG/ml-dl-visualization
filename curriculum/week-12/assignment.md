# 第 12 週作業：CNN 視覺化分析
# Week 12 Assignment: CNN Visualization Analysis

**繳交期限 Due:** 下週上課前
**繳交方式 Submission:** 上傳 Notebook (.ipynb) + PDF 報告至課程平台
**難度等級 Level:** 進階 Advanced

---

## 作業一：CNN 建構與訓練（20%）

使用 PyTorch 在 CIFAR-10 資料集上建構並訓練一個 CNN 模型。

### 要求：
1. 模型至少包含 **3 個卷積層**，每層後接 ReLU 激活與池化
2. 訓練至少 **10 個 epoch**，記錄每個 epoch 的訓練損失 (Training Loss) 與驗證準確率 (Validation Accuracy)
3. 繪製**訓練曲線** (Training Curve)：損失隨 epoch 的變化圖
4. 報告最終的測試準確率 (Test Accuracy)

### 提示：
```python
# 參考架構
class SimpleCNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.conv1 = nn.Conv2d(3, 32, 3, padding=1)
        self.conv2 = nn.Conv2d(32, 64, 3, padding=1)
        self.conv3 = nn.Conv2d(64, 128, 3, padding=1)
        self.pool = nn.MaxPool2d(2, 2)
        # ... 全連接層
```

---

## 作業二：卷積核與特徵圖視覺化（25%）

### Part A：卷積核視覺化（10%）
1. 提取已訓練模型**第一層卷積核**的權重
2. 以網格方式視覺化所有卷積核
3. **回答問題**：你觀察到哪些類型的模式？（如：邊緣、色彩、紋理等）

### Part B：特徵圖視覺化（15%）
1. 選擇 **3 張不同類別**的測試影像
2. 分別提取**淺層（第 1 卷積層）**與**深層（最後卷積層）**的特徵圖
3. 以網格方式顯示每層的前 16 個通道
4. **回答問題**：
   - 淺層與深層的特徵圖有什麼顯著差異？
   - 不同類別的影像在同一層的特徵圖有什麼不同？
   - 哪些通道似乎對特定物件特別敏感？

---

## 作業三：Grad-CAM 分析（30%）

### Part A：基本 Grad-CAM（15%）
1. 對 **5 張不同類別的測試影像**生成 Grad-CAM 熱度圖
2. 將熱度圖疊加在原圖上呈現
3. 確認 Grad-CAM 針對的是**最後一個卷積層**

### Part B：深入分析（15%）
1. 找出模型**預測正確**的 2 張影像和**預測錯誤**的 2 張影像
2. 分別生成 Grad-CAM 熱度圖
3. **撰寫分析報告**（至少 200 字）：
   - 預測正確時，模型關注的區域是否合理？
   - 預測錯誤時，模型關注了哪些錯誤的區域？
   - 這些觀察對模型改進有什麼啟示？
   - 你認為 Grad-CAM 在哪些場景最有價值？（如：醫療影像、自動駕駛等）

---

## 作業四：不同層的 Grad-CAM 比較（10%）

1. 選擇一張測試影像
2. 分別對 **conv1（淺層）**、**中間卷積層**、**最後卷積層（深層）** 生成 Grad-CAM
3. 將三張熱度圖並排呈現
4. **回答問題**：不同層的 Grad-CAM 關注區域有何差異？為什麼會有這樣的差異？

---

## 作業五：綜合思考題（15%）

請以文字方式回答以下問題（每題至少 100 字）：

### Q1：特徵圖的演進
> 請解釋為什麼 CNN 淺層的特徵圖看起來像邊緣偵測的結果，而深層的特徵圖更抽象？這與感受野 (Receptive Field) 有什麼關係？

### Q2：Grad-CAM 的限制
> Grad-CAM 有哪些已知的限制？在什麼情況下，Grad-CAM 的解釋可能不可靠？你知道哪些改進的方法嗎？

### Q3：CNN 可視化的應用
> 舉出至少兩個真實世界的場景，說明 CNN 可視化技術如何幫助改善 AI 系統的可信度 (Trustworthiness) 與安全性 (Safety)。

---

## 進階挑戰（附加分 Bonus，最多 +15%）

### 挑戰 A：遷移學習實驗（+10%）
1. 使用 PyTorch 的 **pretrained ResNet-18** 模型
2. 在一個小型資料集（如 CIFAR-10 的子集，每類 100 張）上進行遷移學習
3. 比較以下兩種策略的測試準確率：
   - 策略 1：凍結所有卷積層，只訓練全連接層
   - 策略 2：微調最後兩個 ResBlock + 全連接層
4. 對兩個模型生成 Grad-CAM 並比較

### 挑戰 B：不同可視化方法比較（+5%）
使用 `pytorch-grad-cam` 套件，對同一張影像比較以下方法的結果：
- Grad-CAM
- Grad-CAM++
- Score-CAM
- Eigen-CAM

簡短說明各方法的差異與優劣。

---

## 評分標準 Grading Criteria

| 項目 | 比重 | 評分重點 |
|------|:---:|---------|
| 程式碼正確性 Code Correctness | 30% | 程式碼可執行、邏輯正確、無明顯 bug |
| 視覺化品質 Visualization Quality | 25% | 圖表清晰、標題/標籤完整、配色適當 |
| 分析深度 Analysis Depth | 25% | 觀察細膩、解釋合理、有自己的思考 |
| 報告結構 Report Structure | 10% | 結構清晰、段落分明、圖文搭配 |
| 程式碼風格 Code Style | 10% | 命名清晰、註解適當、模組化 |

---

## 學術誠信提醒 Academic Integrity

- 你可以參考課程 Notebook 和官方文件，但**必須理解每行程式碼的作用**
- AI 助教可以幫助你除錯和理解概念，但**不能直接複製 AI 生成的完整程式碼作為作業**
- 分析報告必須是**你自己的觀察與思考**
- 若引用他人程式碼或論文，請**明確標註來源**
