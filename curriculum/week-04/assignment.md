# 第 4 週作業：線性回歸 — 損失函數、梯度下降視覺化
# Week 4 Assignment: Linear Regression — Loss Function & Gradient Descent Visualization

**繳交期限 Deadline:** 第 5 週上課前
**繳交方式：** 上傳 Jupyter Notebook (.ipynb) + 反思報告 (.md 或 .pdf)
**配分：** 本週作業佔學期成績 2%

---

## 作業一：手動實作梯度下降 (30%)
### Task 1: Implement Gradient Descent from Scratch

**目標：** 不使用 sklearn，純手動用 Python + NumPy 實作簡單線性回歸的梯度下降。

**要求：**

1. **產生合成資料 (Synthetic Data)**
   - 使用 `np.random.seed(42)` 確保可重現性
   - 產生 100 個資料點，真實關係為 $y = 3x + 7 + \epsilon$，其中 $\epsilon \sim \mathcal{N}(0, 5)$

2. **實作 `gradient_descent()` 函式**
   ```python
   def gradient_descent(X, y, lr=0.01, epochs=100):
       """
       參數:
           X: 特徵矩陣 (n,)
           y: 目標值 (n,)
           lr: 學習率
           epochs: 迭代次數
       回傳:
           w_history: 每次迭代的 w 值列表
           b_history: 每次迭代的 b 值列表
           loss_history: 每次迭代的 MSE 損失值列表
       """
       # 你的實作
   ```

3. **輸出結果**
   - 印出最終的 $w$ 和 $b$（應接近 3 和 7）
   - 畫出損失曲線 (Loss Curve)：x 軸為迭代次數，y 軸為 MSE
   - 畫出散佈圖 + 最終擬合線

**評分標準：**
- 梯度計算正確 (10%)
- 更新公式正確 (10%)
- 圖表清楚標示（標題、軸標籤、圖例） (10%)

---

## 作業二：學習率影響實驗與分析 (25%)
### Task 2: Learning Rate Impact Experiment

**目標：** 實驗不同學習率對梯度下降收斂行為的影響，並撰寫分析。

**要求：**

1. **實驗設計**
   - 使用作業一的資料與梯度下降函式
   - 測試四種學習率：$\alpha \in \{0.001, 0.01, 0.1, 1.0\}$
   - 每種學習率跑 200 次迭代 (epochs)

2. **視覺化**
   - 畫出四條損失曲線在同一張圖上（不同顏色/線型），加上圖例
   - 記錄每種學習率：
     - 是否收斂？
     - 收斂到的最終損失值
     - 大約在第幾次迭代開始穩定

3. **分析報告**（至少 150 字）
   - 描述每種學習率的行為特徵
   - 解釋為什麼 $\alpha = 1.0$ 會發散
   - 說明如何在實務中選擇合適的學習率
   - 提出至少一種改進學習率選擇的方法

**評分標準：**
- 實驗執行正確 (10%)
- 視覺化清晰美觀 (5%)
- 分析深度與正確性 (10%)

---

## 作業三：損失地形視覺化 (20%)
### Task 3: Loss Landscape Visualization

**目標：** 繪製 MSE 損失函數在 $(w, b)$ 參數空間中的 3D 曲面與等高線圖。

**要求：**

1. **3D 損失地形圖**
   - 使用 matplotlib 的 `plot_surface`
   - $w$ 範圍：$[-5, 10]$，$b$ 範圍：$[-5, 15]$，解析度至少 50x50
   - 加上適當的軸標籤與標題

2. **等高線圖 + 梯度下降軌跡**
   - 繪製損失函數的等高線圖
   - 在等高線圖上疊加作業一中梯度下降的 $(w, b)$ 軌跡
   - 標示起始點與終點
   - 用箭頭或不同大小的點表示移動方向

3. **思考題**（寫在 Notebook 的 Markdown cell 中）
   - 為什麼 MSE 的損失地形是碗狀的？
   - 如果兩個特徵的尺度差異很大（例如一個範圍是 [0, 1]，另一個是 [0, 10000]），等高線會變成什麼形狀？這對梯度下降有什麼影響？

**評分標準：**
- 3D 圖正確且美觀 (8%)
- 等高線圖 + 軌跡正確 (8%)
- 思考題回答有深度 (4%)

---

## 作業四：殘差分析 (15%)
### Task 4: Residual Analysis

**目標：** 對線性回歸模型進行殘差診斷。

**要求：**

1. **使用 sklearn 的 LinearRegression**
   - 用 sklearn 建立線性回歸模型，對作業一的資料進行擬合
   - 計算殘差 $e_i = y_i - \hat{y}_i$

2. **繪製以下圖表：**
   - **殘差 vs 預測值圖 (Residuals vs Fitted)：** 檢查是否有模式
   - **殘差直方圖 (Histogram)：** 檢查殘差分布是否接近常態
   - **Q-Q 圖 (Q-Q Plot)：** 使用 `scipy.stats.probplot` 或手動繪製

3. **計算並報告評估指標：**
   - MSE、RMSE、MAE
   - $R^2$ 與 Adjusted $R^2$
   - 解釋每個指標的意義

4. **診斷結論**（至少 100 字）
   - 殘差是否呈現隨機分布？
   - 是否有異質變異性的跡象？
   - 常態性假設是否成立？

**評分標準：**
- 圖表正確 (6%)
- 指標計算正確 (4%)
- 診斷分析有見解 (5%)

---

## 作業五：反思報告 (10%)
### Task 5: Reflection Report

**目標：** 回顧本週學習內容，進行深度反思。

**要求：** 撰寫 200-400 字的反思報告，涵蓋以下面向：

1. **概念理解**
   - 本週最有收穫的概念是什麼？為什麼？
   - 梯度下降的直覺你是否真正理解了？用你自己的話解釋給一個沒學過的同學聽

2. **實作體驗**
   - 手動實作梯度下降的過程中，遇到了什麼困難？如何解決的？
   - 視覺化對你理解損失地形與梯度下降有什麼幫助？

3. **連結思考**
   - 梯度下降不只用在線性回歸，你認為它在深度學習中會扮演什麼角色？
   - 本週的內容如何與前幾週（監督式學習、資料分割）的概念串聯？

4. **自我評估**
   - 你認為自己本週學習的投入程度如何？（1-5 分）
   - 還有哪些概念需要進一步釐清？

**評分標準：**
- 反思深度與真誠度 (5%)
- 概念理解的正確性 (3%)
- 文字表達清晰度 (2%)

---

## 加分題 Bonus (最多 +10%)

### Bonus 1: Mini-batch GD 實作 (+5%)
實作 Mini-batch Gradient Descent（batch size = 16），與 Batch GD 在同一張等高線圖上比較兩者的軌跡差異。

### Bonus 2: 多項式回歸過擬合實驗 (+5%)
使用 sklearn 的 `PolynomialFeatures`，分別擬合 degree = 1, 3, 5, 10, 15 的多項式回歸，在同一張圖上比較擬合效果，並計算訓練/測試 MSE。畫出 degree vs train/test MSE 的曲線，討論偏差-變異權衡 (Bias-Variance Tradeoff)。

---

## 提交檢查清單 Submission Checklist

- [ ] Jupyter Notebook 包含所有必要的 code cells 與輸出
- [ ] 所有圖表都有標題、軸標籤、圖例
- [ ] Notebook 可以從頭到尾重新執行 (Run All) 不出錯
- [ ] 反思報告已完成（含在 Notebook 最後或獨立檔案）
- [ ] 隨機種子 (Random Seed) 已固定，結果可重現
- [ ] 分析文字有足夠深度，非流水帳式敘述

---

## AI 助教使用提醒

- 你可以向 AI 助教詢問概念性問題（例如「梯度的方向代表什麼意義？」）
- AI 助教**不會**直接給你程式碼答案，但會引導你思考
- 建議在遇到困難時先嘗試 15 分鐘，再向 AI 助教求助
- 請在反思報告中記錄你與 AI 助教的互動經驗（如果有的話）
